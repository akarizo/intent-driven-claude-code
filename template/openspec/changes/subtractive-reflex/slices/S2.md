# S2 · 执行体与评审员的减法契约

**目标**：把「先查已有 / 修在汇流处 / 切角标天花板」放进执行体每次开工必读的契约，把「症状修复」加进评审维度。判断前移到动手前，评审退为兜底。

**deps: 无**（与 S1 同 wave 并行，owns 不相交）

## owns

- `template/.claude/agents/slice-executor.md`（39 行，纪律段 +3 条 bullet）
- `template/.claude/agents/code-reviewer.md`（86 行，审查 checklist 的「正确性」维度 +1 条）
- `tests/test_agents_workflow.py`（骨架已在，去掉 `xfail` 并写实断言）

## 事实基线（实测，勿重新调研）

- `slice-executor.md` 的 `## 纪律` 段现有 6 条 bullet：只写 owns 内文件 · 一轮多动作 · 不加 `cd &&` 前缀 · TDD · 改了骨架断言要申报 · 每切片一个 commit · 预算。新增三条**追加在 TDD 之前**（减法发生在写代码前，TDD 之后是执行细节）。
- `code-reviewer.md` 的 `## 审查 checklist` 现有 7 个维度：正确性 · 安全 · 数据完整性 · 规格符合 · 测试质量 · 可维护性 · 最小改动。**可维护性维度已含「重复代码」「未要求的抽象 / 过度设计（YAGNI）」——本切片不动它**，只在「正确性」维度加「症状修复」。
- `tests/test_agents_workflow.py::test_executor_agent_contract`（63–80 行）**已在对正文做内容断言**（`"一轮多动作" in body` 等），扩断言即可，无需新测试文件。
- `tests/test_agents_workflow.py::test_reviewer_no_rerun`（83–92 行）断言 `modes == {"full","follow-up"}` 且 `"水位线" not in body`。

## 契约（措辞即断言，勿自由改写）

`slice-executor.md` 纪律段新增三条（原文照此落，测试按关键短语断言）：

- **先查已有再写**：写新函数 / 类型 / 常量前，先在项目里查有没有同义实现（`cx symbols --name` 或 Grep）；命中就复用，重写一份等价物是最常见的 slop。
- **修在汇流处**：改函数体前先 grep 它的**全部 caller**；只修工单点名的那条路径会留下兄弟 caller 仍坏着——一个共享函数里的 guard 比每个 caller 一个 guard 的 diff 更小。
- **切角要标天花板**：故意切了真角的简化（全局锁 · O(n²) 扫描 · 朴素启发式）留一行 `ceiling: <限制> -> <升级条件/路径>`；G8 只校验标了的格式完整性，不逼你标。

`code-reviewer.md` 的「正确性」维度新增：

- **症状修复**：只修了工单点名的路径，同类输入经其他 caller 仍会失败。

## 断言面（写实骨架时照此）

| 断言 | 目标 |
|---|---|
| `"先查已有" in body` · `"复用" in body` | 第一条纪律在位 |
| `"全部 caller" in body` · `"汇流处" in body` | 第二条纪律在位 |
| `"ceiling:" in body` | 第三条纪律在位 |
| `"LOC" not in body` | 不含长度类判据（ADR 的永久拒绝项） |
| `"症状修复" in reviewer_body` · `"caller" in reviewer_body` | 评审维度在位 |
| `"重复代码" in reviewer_body` · `"过度设计" in reviewer_body` | 既有可维护性维度一条不少 |

## 坑

⚠ `test_executor_agent_contract` 与 `test_reviewer_no_rerun` 的既有断言一条不得动（它们锁的是别的 change 的契约）。只**新增**测试函数，不改既有函数体。

## scenarios

| scenario | 测试 |
|---|---|
| executor-carries-subtractive-rules | `tests/test_agents_workflow.py::test_executor_carries_subtractive_rules` |
| reviewer-flags-symptom-fix | `tests/test_agents_workflow.py::test_reviewer_flags_symptom_fix` |

## verify

```bash
python3 -m pytest -q tests/test_agents_workflow.py
```

两个 agent 文件的 frontmatter 必须仍是合法 YAML 且字段不变（`test_executor_agent_contract` 会验）。
