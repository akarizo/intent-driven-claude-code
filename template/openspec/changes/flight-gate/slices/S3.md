# S3 · 铁律与文档：CLAUDE.md · snippet · README · WORKFLOW_zh

**目标**：把"起飞前执行模型须经人确认"写进铁律，把阶段边界写进面向人的文档。

## owns

- `CLAUDE.md`（仓库根）
- `template/CLAUDE.md.snippet`
- `README.md`
- `docs/WORKFLOW_zh.md`
- `tests/test_docs_iron_rules.py`（骨架已在，去掉 xfail 标记即解锁；文件里其它测试不要动）

## 依赖 S1 的接口

只需知道 flag 的拼写与语义：`--confirm-model=<alias>`，alias ∈ `opus` / `sonnet` / `haiku` / `fable`，由 `UserPromptSubmit` hook 强制、证据是人自己 prompt 里的这段文本。

## 要做的改动

### 1. 铁律（仓库根 `CLAUDE.md` 与 `template/CLAUDE.md.snippet` 措辞一致）

在"两处人类审批不可省"那一条里补：**起飞前执行模型须经人确认，证据是人 prompt 里的 `--confirm-model=<alias>`**；模型不得代填、不得推断。

⚠ 只改这一条相关的措辞。PR #29 已在该条加过"判据不得由模型自证"，**保留不动**，不要重写整条铁律。

### 2. `README.md` · `docs/WORKFLOW_zh.md`

补一张阶段边界表（三行即可）：

| 阶段 | 可产出 | 禁止 |
|---|---|---|
| `/opsx-explore` | `proposal` · `design` · `specs` | `slices.json` · `tasks.md` · `slices/*.md` · baseline · 实现类子 agent |
| `/opsx-propose` | 全部工件 + 切片计划 + baseline + `spec.html` | 在同一轮继续 apply |
| `/opsx-apply` | 实现与门禁 | 起飞前不带 `--confirm-model=` |

并说明第一次敲 `/opsx-apply` 必被拦下一次是设计（给人看当前主模型），不是故障。

## scenarios（原文见 `specs/takeoff-model-confirm/spec.md`）

| scenario | 测试 |
|---|---|
| takeoff-model-confirm#iron-rule-records-confirm | `tests/test_docs_iron_rules.py::test_iron_rule_records_confirm` |

## verify

```bash
python3 -m pytest -q tests/test_docs_iron_rules.py
```

⚠ 文档禁日文假名 / 韩文谚文（仓库纪律）。
