# S2 · test-evidence：change 定位 标记 → 分支名 → 唯一候选 → 不写

**目标**：没有 `.openspec-slice` 标记时不再猜 change。设计 D5。

**deps: 无**（wave 1）

## owns

- `template/.claude/hooks/test-evidence.py`
- `tests/test_evidence_timeline.py`（3 条骨架已在）

## 事实基线

- `find_change(root)`（37–63 行）：标记 → 遍历 `openspec/changes/*`（跳过 `archive`）取**第一个** `tasks.md` 有 `- [ ]` 的 change。返回 `(change_dir, slice_id)`。
- 主流程 `main()` 82–91 行：`root = toplevel(cwd)`；`change_dir` 为空即 return。fail-open：任何异常静默。
- 测试基座：`tests/test_evidence_timeline.py` 的 `marker_repo(git_repo)` 与 `bash_event(cmd, …, cwd)`；`conftest.git_repo` 初始分支 `main`。

## 契约

```python
BRANCH_PREFIX = "worktree-"

def current_branch(root) -> str | None          # git branch --show-current，失败返回 None
def find_change(root):
    # 1 标记（不变）
    # 2 分支：name = branch[len(BRANCH_PREFIX):]；os.path.isdir(openspec/changes/<name>) → (that, "-")
    # 3 候选：收集所有 tasks.md 含未勾选项的 change；恰好 1 个 → (it, "-")；否则 (None, None)
```

只改 `find_change`（与新增 `current_branch`），`main()` 与 `TEST_RE` / `FAIL_RE` 不动。

## 坑

- ⚠ 分支名可能为空（detached HEAD）：`git branch --show-current` 输出空串，按 None 处理，直接进第 3 级。
- ⚠ 测试里切分支用 `git checkout -q -b worktree-c`（`conftest.git`）；`a` 与 `c` 两个 change 都要有未勾选 tasks.md，证明分支优先于字母序。

## scenarios

| scenario | 测试 |
|---|---|
| evidence-resolves-change-from-branch | `::test_evidence_resolves_change_from_branch` |
| evidence-refuses-ambiguous-change | `::test_evidence_skips_ambiguous_change` |
| evidence-single-candidate-fallback | `::test_evidence_single_candidate_fallback` |

## verify

```bash
python3 -m pytest -q tests/test_evidence_timeline.py
```
