# S3 · 文档：apply 传分支名、回退 effort 取 frontmatter；取 diff 拉不到回退本地 diff

**目标**：apply 命令与同名 skill 的工作流参数用 `branch` 取代 `expectHead`，回退路径写明 `--expect-branch` 与原样返回（设计 D1 文档侧）；回退派发不再把 effort 写成 Agent 参数（设计 D6）；`pr-ship.md` step 8 与 `code-reviewer.md` 评审流程区分「为空 / 拉不到 / 都拉不到」（设计 D4）。

**deps: -**（wave 1）

## owns

- `template/.claude/commands/opsx-apply.md`
- `template/.claude/skills/openspec-apply-change/SKILL.md`
- `template/.claude/commands/pr-ship.md`
- `template/.claude/agents/code-reviewer.md`
- `tests/test_template_docs.py`（4 条骨架已在文件末尾）

## 事实基线（main a88a97b）

- `opsx-apply.md` 57 行：Workflow `args` 含 `expectHead: "<git rev-parse --short=10 HEAD>"`；59 行回退段含 `effort: high`（执行体、评审员）、`effort: low`（integrator）与「prompt 首行带 `expectHead` 第零步基分支校验」。
- `openspec-apply-change/SKILL.md` 65 行、67 行：同上（同句镜像）。
- `pr-ship.md` 115–140 行是 step 8；评审 prompt 模板里 130 行「取 diff: `gh pr diff <num>`（GitHub）或 `glab mr diff <num>`（GitLab）。」、131 行「diff 为空或拉不到 → 报告"无变更"并停止。」
- `code-reviewer.md` 32 行（「## 评审流程」第 2 条）：「diff 为空或拉不到 → 报告「无变更」并停止，不要硬凑。」
- 既有测试锁（不能打破）：
  - `test_apply_command_zero_prompts`：`slice-gate.py lint` 到 `/pr-ship` 之间含 `Workflow`、`useAgentTypes`、`回退`、`session-decompose`、`timeline.py record approve`，且无 `AskUserQuestion`。
  - `test_command_skill_sync`：命令与 skill 提到的 hook 脚本名集合一致。
  - `tests/test_agents_workflow.py::test_apply_docs_mirror_retry_and_preflight`：两者 lint 之后跑 preflight；回退含 `cherry-pick` 与 `--base`。
  - `tests/test_agents_workflow.py::test_reviewer_no_rerun` / `test_reviewer_flags_symptom_fix`：`code-reviewer.md` 仍含 `不重跑测试`、`evidence.log`、`findings`、`severity`、`症状修复`、`caller`、`重复代码`、`过度设计`，模式仍只有 full / follow-up。
  - `tests/test_ship_verdict.py::test_pr_ship_bidirectional_transition` / `test_apply_writes_full_findings_and_calls_ship`：pr-ship 的 step 7 / 10 锚点与 apply 收口段不动。
  - `test_pr_ship_single_review`：`AskUserQuestion` ≤ 1 处；`model: "<main>"` ≥ 2 处。

## 契约（文案可微调，测试锚点必须在）

- apply 命令 / skill 的 Workflow `args`：`expectHead: …` → `branch: "<git branch --show-current>"`（在本 change 的 worktree 里取）。
- 回退段：
  - Agent 派发只写 `subagent_type` / `model`（多切片 wave 加 `isolation: worktree`），括注「Agent 工具没有 effort 参数，effort 由 agent frontmatter 决定：执行体 / 评审员 high，integrator low」。
  - 「prompt 首行带 `expectHead` 第零步基分支校验」→「start 带 `--expect-branch <branch>`，由 `slice-gate.py` 校验 worktree 是否从 change 分支最新 commit 分叉；start 非 0 则执行体把它打印的 JSON 原样返回」。
  - integrator 合回处补「只合回、不跑 final」。
- `pr-ship.md` step 8 评审 prompt：取 diff 首选 `gh pr diff` / `glab mr diff`；拉取失败（例如 diff 过大 HTTP 406）→ `git fetch origin <target>` 后 `git diff origin/<target>...HEAD`；diff 确实为空 → 报告"无变更"并停止；两种都拉不到 → 报告"取 diff 失败"并停止，不得报告无变更。
- `code-reviewer.md` 评审流程第 2 条同义改写：为空 → 报告「无变更」；命令失败 → 按 prompt 给的回退命令再取；仍拉不到 → 报告「取 diff 失败」并停止，不得报告「无变更」，不要硬凑。

## 坑

- ⚠ 两份 apply 文档同句同改（`test_command_skill_sync` 只比 hook 脚本名，语义漂移要自己对照）。
- ⚠ 不要在 opsx-apply.md 的 lint 到 pr-ship 区间写 `AskUserQuestion` 字样。
- ⚠ 路由表的 effort 列保留（它描述各角色的 effort 水位），只改回退段里「当作 Agent 参数」的写法。

## scenarios

| scenario | 测试 |
|---|---|
| slice-base-check#apply-docs-pass-branch | `tests/test_template_docs.py::test_apply_docs_pass_branch` |
| flight-doc-contracts#fallback-effort-from-frontmatter | `tests/test_template_docs.py::test_fallback_effort_from_frontmatter` |
| review-diff-fallback#pr-ship-review-falls-back-to-local-diff | `tests/test_template_docs.py::test_pr_ship_review_diff_fallback` |
| review-diff-fallback#reviewer-distinguishes-empty-and-unreachable | `tests/test_template_docs.py::test_reviewer_diff_empty_vs_unreachable` |

## verify

```bash
python3 -m pytest -q tests/test_template_docs.py tests/test_agents_workflow.py tests/test_ship_verdict.py
```
