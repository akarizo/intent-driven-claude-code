<!-- intent-driven:begin -->
## 任务分级 · 何时必须走工作流
**中级+ 必须走 5 工件（`proposal → specs → design → adr → tasks`），mini 才可跳过**。源码写入受 PreToolUse 门禁 `.claude/hooks/intent-gate.py` 强制。

- **中级+（强制 `/opsx-propose`）**：新 capability · 新公共 API/命令 · 跨模块 · 引入新抽象或依赖。
- 中级+ 续：改公共契约（签名/返回/错误码/**数据投影·序列化**）· 影响数据模型或迁移 · 任何长期不可逆设计决策。
- **mini（可跳过）**：文档/注释 · 依赖升级 · 配置值 · 单文件无行为变化 hotfix · 一次性脚本。
- mini 改源码前先 `/opsx-mini "<理由+范围>"` 留痕：24h 内放行 scope 文件，完后 `--done`。
- ⚠ **原生 plan mode 的 markdown plan + ExitPlanMode ≠ 工作流**：approve 一份 plan 不满足中级+ 工件要求，5 工件照欠

## Intent-Driven 工作流 · 飞行模式
命令前缀 `/opsx-*`；机制细节在各自 skill 里，此处只留必须常驻的：

- **Worktree 隔离**：每 change 一间 `.worktrees/<change>/` + `worktree-<change>` 分支。
- 5 工件 + `spec.html` + 实现**全落 worktree 内**，主仓库根随时干净。详 `.claude/skills/openspec-git-discipline/`
- **飞行流程**：tasks 阶段生成 `slices.json`（切片 owns/deps/verify/scenarios）+ 切片包；`/opsx-apply` 按 wave 并行派 `slice-executor`，`slice-gate.py` 机械门禁；评审离关键路径 + 一次批量修复；收口打印飞行记录。legacy 逐 task 守门用 `--gate=per-task` 显式选。
- **Git 授权边界**：建 worktree 可自动做；**merge / push / 删 worktree / commit 一律交你授权**，不代劳
- **ADR 不可改**：入 `openspec/adr/` 后需变更则 supersede 旧版，禁原地改
- **TDD**：写实现前先写失败测试，红→验红→绿→验绿→重构；单测先写 `# Given: / # When: / # Then:` 三段中文注释（详见 `.claude/skills/test-driven-development/`）
- **两处人类审批不可省**：起飞批准（发起后你自己回一句短确认）与 PR review
- **落点收敛**：ADR → `openspec/adr/`；未成 change 的探索稿 → `openspec/superpower/`；每 change 产物 → 其 worktree

## 铁律（零容忍）
1. 先意图后代码：中级+ 无工件不写源码，门禁只加严不绕过。
2. 规格即验收：scenario 测试骨架全部转 pass 才算 apply 完成。
3. TDD 与留痕：先失败测试后代码；测试运行由 hook 留痕，不接受自述。
4. 独立评审必有：CRITICAL/HIGH 未闭环不得非 draft PR。
5. 门禁红不收口：slice-gate / final gate 红时禁勾选、禁声明完成。
6. Git 边界：一 change 一 worktree，不自动 merge / push / 删；ADR 只 supersede。
7. 两处审批不可省：起飞批准与 PR review；起飞是两段式握手——发起 ≠ 批准：起飞指令只是发起，批准是其后人自己发的一句短确认，模型不得代填；由 takeoff-gate.py 校验人类消息证据（模型自证无效）。
8. 度量必打印：apply 收口打印飞行记录，无数据不称提速。
9. 零 token 优先：能脚本 / hook 判定的不交给模型。
10. 回退必存在：机制失效时回退路径语义一致。
11. 模型显式路由：执行体 / 评审员 = 会话主模型 high，机械角色 = sonnet low；派发不留空 `model`；收口打印各角色实际模型。

## 记忆分流（写 CLAUDE.md 前必读）
权威全文 `.claude/claudemd-standard.md`；`/claudemd-commit` 沉淀、`/claudemd-distill` 重排、`claudemd-lint` 拦截。

- **准入四问**（任一 ✗ 即不入）：① 会命中吗 ② 不写会做错吗 ③ 做错会**静默**吗 ④ 能写成测试/lint/hook 吗。
- 第 ④ 问答「能」→ **去写那个**，本文件只留一行防线索引。
- ⚠ **静默失败是 CLAUDE.md 唯一不可替代的辖区**；会报错的约束，循环自己会教。
- **不默认落 CLAUDE.md**：每会话必命中 → CLAUDE.md；只治理某片目录 → `.claude/rules/*.md` + `paths:`（按读文件触发）。
- **memory 是第五载体**（索引常驻·正文按需 recall）：不写会**做错事** → CLAUDE.md；只是**多花时间** → memory。写一条 = 给所有会话永久加一行索引，**10 次会话用不到 1 次就别写**；`memory-lint` 守闭合。
- 多步流程 · 深度契约 → skill；可断言 → 测试。**「重要」≠「该常驻」。**
- **预算按字节**：根 ≤8KB（不含本注入段）/ 子 ≤16KB / 叶 ≤6KB / 单行 ≤200B。超线**当场**减（加一减一），禁推给以后。
- ⚠ 指针写 `` `@path` ``（反引号内）**不会被加载**，是死记号；裸 `@path` 才是 launch 急切导入 —— 写指针别带 `@`。

## Model 路由（按角色显式声明）
> Claude Code v2.1.251 起 subagent 模型解析顺序 = 调用时 `model` 参数 > agent frontmatter > `CLAUDE_CODE_SUBAGENT_MODEL` env > 主会话（来源：code.claude.com/docs/en/sub-agents "Choose a model"，2026-09-10 核实）。env 只是**默认值**；需强制统一才设 `CLAUDE_CODE_SUBAGENT_MODEL_FORCE`（模板不设）。fork 恒随主会话模型。

- **飞行角色**：slice-executor / code-reviewer = 会话主模型 + effort high；integrator 等机械角色 = sonnet + effort low。`/opsx-apply` 以 `args.models` 显式传入，脚本缺参即拒绝起飞。
- **主模型判定禁自述**：`<main>` 由 `.claude/hooks/session-model.py` 读会话转录判定；判定不出停飞，人工 `--model=<alias>`。
- **任何 Agent 派发不得省略 `model`**：留空就落到 env 默认，这正是 2026-09-10 实测「实现 / 评审 / 修复全在 Sonnet」的成因。
- **确需主模型级子任务**（复杂重构 · 架构决策 · 难 bug 根因）：主会话直接做，或派发时显式 `model: <主模型别名>`。
- **主会话自查**：低推理命令（`/opsx-explore` `/opsx-sync` `/claudemd-commit`）先手动 `/model sonnet`——env 管不到主会话。

## 上下文纪律
> `[1m]` 长上下文失去 200k autocompact 兜底 → 闸门全靠人工。

- **compact/clear 是唯一闸门**：>150k 主动 `/compact`；换任务用 `/clear` 而非同会话续命（不管就一路涨到 500k+，每条消息反复 cache read 几十万 token）。
- **挂机会话跨天必重启**：`model` 配置不热更新（仅 `env` 热更新），旧会话恒停在启动时的模型层级。
- **禁马拉松 subagent**：单任务 scoped；预计超 ~100 轮或 150k 上下文先拆阶段、每阶段新开 agent 收束。
<!-- intent-driven:end -->
