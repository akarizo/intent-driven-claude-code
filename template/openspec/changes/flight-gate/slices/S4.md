# S4 · 铁律与文档：CLAUDE.md · snippet · README · WORKFLOW_zh

**目标**：把"起飞是两段式握手"写进铁律，把阶段边界写进面向人的文档。

## owns

- `CLAUDE.md`（仓库根）
- `template/CLAUDE.md.snippet`
- `README.md`
- `docs/WORKFLOW_zh.md`
- `tests/test_docs_iron_rules.py`（骨架已在，去掉 xfail 标记即解锁；文件里其它测试不要动）

## 依赖 S1 的接口

只需知道握手语义：起飞指令（一行自然语言 或 `/opsx-apply`）= 发起；批准 = 发起之后一条 ≤ 40 字、含批准词、不含 change 名与路径的人类消息；停下时由 `takeoff-gate.py` 展示主模型与规模。

## 要做的改动

### 1. 铁律（仓库根 `CLAUDE.md` 与 `template/CLAUDE.md.snippet` 措辞一致）

在"两处人类审批不可省"那一条里补：**起飞为两段式握手——发起 ≠ 批准，批准是发起之后人自己发出的短确认**；模型不得代填、不得把发起当批准。

⚠ 只改这一条相关的措辞。PR #29 已在该条加过 `takeoff-gate.py` 机械校验、在铁律 11 加过"禁自述"，**保留不动**，不要重写整条。

### 2. `README.md` · `docs/WORKFLOW_zh.md`

补一张阶段边界表（三行即可）：

| 阶段 | 可产出 | 禁止 |
|---|---|---|
| `/opsx-explore` | `proposal` · `design` · `specs` | `slices.json` · `tasks.md` · `slices/*.md` · baseline · 实现类子 agent |
| `/opsx-propose` | 全部工件 + 切片计划 + baseline + `spec.html` | 在同一轮继续 apply |
| 起飞 | 实现与门禁 | 发起后未经人短确认即派发 |

并说明起飞会被停一次是设计（给人看当前主模型与规模），不是故障；以及 propose 收尾会给出一行可直接复制的起飞指令。

## scenarios（原文见 `specs/takeoff-model-confirm/spec.md`）

| scenario | 测试 |
|---|---|
| takeoff-model-confirm#iron-rule-records-handshake | `tests/test_docs_iron_rules.py::test_iron_rule_records_handshake` |

## verify

```bash
python3 -m pytest -q tests/test_docs_iron_rules.py
```

⚠ 文档禁日文假名 / 韩文谚文（仓库纪律）。
