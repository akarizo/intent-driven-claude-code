# S6 · 文档与铁律

**目标**：文档描述飞行流程；铁律落进两处 CLAUDE.md；旧模式降级为 legacy。

## 产物（owns）

### 仓库根 `CLAUDE.md`（本仓库自己的记忆，≤ 8KB）

保留现有两行；新增：

```
## 仓库铁律（零容忍：任何提速手段不得破坏）
1. 先意图后代码：中级+ 变更无工件不写源码；intent-gate 只能加严不能绕过。
2. 规格即验收：每个 scenario 有测试骨架与映射；apply 完成 = 全部 scenario 测试 pass 且骨架标记已去。
3. TDD 与留痕：生产代码前必有先失败的测试；测试运行由 hook 留痕，不接受自述。
4. 独立评审必有：每个 change 至少一次不带主会话立场的 reviewer；CRITICAL/HIGH 未闭环不得非 draft PR。
5. 门禁红不收口：slice-gate / final gate 红时禁止勾选、禁止声明完成、禁止 PR。
6. Git 边界：每 change 一间 worktree；不自动 merge、不推 main、不删 worktree；ADR 不可改只 supersede。
7. 两处人类审批不可省：飞行计划批准（运行 /opsx-apply）与 PR review。
8. 度量必打印：每次 apply 收口打印飞行记录；无数据不得声称提速。
9. 零 token 优先：能用脚本 / hook 判定的不交给模型。
10. 回退必存在：新机制失效时回退路径语义一致（Workflow → Agent 派发；脚本渲染 → 保留占位）。

## 本仓库开发纪律
- 命令与同名 skill 同改，禁漂移；`python3 -m pytest -q tests` 全绿；`cd template && openspec schema validate intent-driven` 绿；`.claude/hooks/*.py` 可 compileall。
- 回复与文档禁日文假名 / 韩文谚文。
- change 工件落 `template/openspec/changes/<name>/`，实现在 `.worktrees/<name>/`；PR 前预合并 main。
```

### `template/CLAUDE.md.snippet`

- 「Intent-Driven 工作流」段改写为飞行流程要点（slices.json / 切片包 / 门禁 / workflow / 评审离路径 / 飞行记录 / 两处审批）；删逐 task 守门与水位线；新增「铁律」段（上面 1–10 条精简到每条一行）。
- 「Model 路由」段改为：v2.1.251 起 参数 > frontmatter > env > 主会话；env 只是默认；需强制才设 `_FORCE`（模板不设）；fork 恒主模型。删「传 model 白传」。
- 字节预算 ≤ 8KB（不含 marker）；一事一行。

### `README.md`、`docs/WORKFLOW_zh.md`

- 流程图与「阶段 2 实现」改为飞行流程；守门分层表与水位线段删除或移至「legacy 模式」小节（保留一段说明 `--gate=per-task` 与其实测成本：每 task 22–106 min）；「1 个 subagent」改为三个 agent；命令 / skill 计数更新；新增「Workflow 前置条件」（付费计划、Pro 需打开、allow 规则）。
- 实测数据可引用 `docs/apply-speed-redesign.html`。

### legacy 迁移

- `git mv template/.claude/skills/openspec-subagent-apply-change template/.claude/skills/legacy/openspec-subagent-apply-change`；文件顶部加一段「legacy：默认流程已改为飞行模式，本 skill 仅在 `/opsx-apply --gate=per-task` 时按路径读取」。

### `install.sh`

- 确认 `copy_tree "$TEMPLATE_SRC/.claude"` 覆盖 agents / workflows / hooks（已覆盖则只在注释与 README 里写明）；结尾提示语增加「把测试命令加入 allow 规则，避免 Workflow 被权限提示暂停」。

### `tests/test_docs_iron_rules.py`（新）

断言：两处 CLAUDE.md 含 10 条铁律关键短语；README / WORKFLOW 不在非 legacy 段把「review 水位线」描述为默认；snippet 含 v2.1.251 表述且不含「压过一切」；legacy 目录存在且 opsx-apply.md 提及 `--gate=per-task`（该文件由 S5 改，测试只读）。
